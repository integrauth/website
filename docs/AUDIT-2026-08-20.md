# Website & Academy Audit — 2026-08-20

> Working state file for the audit-and-improve session on branch `claude/website-academy-audit-l9duf2`.
> Purpose: survive context compaction — every phase's status and findings land here as they happen.
> Deliverable at the end: fixes + new interactive content, minified, versions bumped, PR to main.

## Scope (confirmed with Akhil, 2026-08-20)

1. Full audit of website + academy; fix confirmed bugs (front-end AND `src/lib/server/*`, but no
   schema/migration or secrets-policy changes).
2. New interactive content — all three surfaces:
   - more Challenge-mode scenarios (currently 5),
   - new Flow Explorer flows (currently 12),
   - a new hub-level practice widget reusing progress/reset/sync patterns.
3. New lessons only if the audit finds real curriculum gaps (all patterns followed: stable ids,
   DOM order, TOC, stat chips, exam pool, quiz handoffs).
4. End state: minify CSS/JS/HTML, bump `?v=` + `acad-build` + `academy-version.txt` together,
   verify with Playwright (4 themes), push branch, open PR to main.

## Phase status

- [x] Phase 0 — recon: branch == origin/main (aef57d3), tree clean, versions v=5.76 / auth v=2.6 / acad-build 5.76.
- [ ] Phase 1 — parallel read-only audit (6 agents: academy HTML, functions.js, academy-labs.js+exam sync, server, auth+cross-page, live Playwright smoke). Findings → section below.
- [ ] Phase 2 — triage findings, pick fixes + content plan (recorded below before implementation).
- [ ] Phase 3 — implement fixes.
- [ ] Phase 4 — implement new content (challenges, flows, hub widget, lessons-if-gaps).
- [ ] Phase 5 — minify + version bumps + full Playwright verify + typecheck + control-byte check.
- [ ] Phase 6 — commit(s), push, PR.

## Facts pinned during recon

- HTML files are minified IN PLACE (academy.html is one line, ~?KB; index.html 42 lines) — the
  minified HTML *is* the source. Edit with care, re-minify CSS/JS only (HTML edits are already min).
- Current counts (verified): 99 `AcadLabs.register` calls in academy-labs.js, 99 unique
  `data-lab` values in academy.html, stat chips say "99 hands-on labs" / "133 lessons" /
  "12 tracks" / "120+ diagrams" on both academy + index.
- academy-auth.js has its own version line (v=2.6) separate from the 5.76 pair.
- Baseline `npm test`: 38/38 pass (tests/: certificate-serial, exam-draw, login-origin,
  minified-assets, progress-reset, sweep-claim). `npm run` has build:css/js (incl. auth)/html
  scripts and `worker:typecheck`; node_modules present.
- Six audit agents launched (academy HTML structure; functions.js; academy-labs.js+exam-key sync;
  server/Worker; academy-auth.js+cross-page; Playwright live smoke). All read-only.

## Audit findings (Phase 1)

### Agent C — academy-labs.js + exam-key sync (DONE, clean)

No real bugs. Verified: 99 unique `AcadLabs.register` ids; 12 well-formed flows; 5 challenges
(refs r1-bff, w2-wif, r3-tenancy, atk2-fatigue, c3-social — all valid); exam pool 96 Qs, 8/track,
**all 96 (id, a) pairs byte-equal with server EXAM_ANSWER_KEY**; client submits pre-shuffle `oi`,
server grades — correct end to end; ATTEMPTS_PER_DAY 3 == server; no old certId remnants; timers
all cleaned on Reset; no network calls outside lab-exam (which only uses AcademyAuth); no
innerHTML/XSS sinks (only `innerHTML=''` clears + `el()` textNode builder); TOTP/k-anonymity/
ECDSA/salted-SHA-256 crypto all correct; RFC status codes spot-checked correct (12 labs).
Note: lab-exam ALSO has a "finish every lesson" gate before the sign-in gate (existing behavior,
not in CLAUDE.md's description).

Cosmetic only: (1) dead `answered` var in lab-challenge (~L10799); (2) `httpCard()` buckets ≥400
into class `s4xx` — fine today (no 5xx used), rename only if adding 5xx cards.

Planning input: full lab inventory captured (99 labs) — challenges currently cover BFF storage,
WIF, tenancy, MFA fatigue, social engineering; flows cover oidc-code, webauthn, refresh-rotation,
dpop, backchannel-logout, saml-sp, saml-idp, client-creds, device, token-exchange, ciba,
scim-provision.

_(other agent reports pending)_

## Decisions (Phase 2)

_(pending)_

## Implementation log (Phases 3–5)

_(pending)_
