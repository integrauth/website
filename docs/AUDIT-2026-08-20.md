# Website & Academy Audit — 2026-08-20

> Working state file for the audit-and-improve session on branch `claude/website-academy-audit-l9duf2`.
> Purpose: survive context compaction — every phase's status and findings land here as they happen.
> Deliverable at the end: fixes + new interactive content, minified, versions bumped, PR to main.

## Scope (confirmed with Akhil, 2026-08-20)

1. Full audit of website + academy; fix confirmed bugs (front-end AND `src/lib/server/*`, but no
   schema/migration or secrets-policy changes).
2. New interactive content — all three surfaces:
   - more Challenge-mode scenarios (currently 5),
   - new Flow Explorer flows (currently 12),
   - a new hub-level practice widget reusing progress/reset/sync patterns.
3. New lessons only if the audit finds real curriculum gaps (all patterns followed: stable ids,
   DOM order, TOC, stat chips, exam pool, quiz handoffs).
4. End state: minify CSS/JS/HTML, bump `?v=` + `acad-build` + `academy-version.txt` together,
   verify with Playwright (4 themes), push branch, open PR to main.

## Phase status

- [x] Phase 0 — recon: branch == origin/main (aef57d3), tree clean, versions v=5.76 / auth v=2.6 / acad-build 5.76.
- [ ] Phase 1 — parallel read-only audit (6 agents: academy HTML, functions.js, academy-labs.js+exam sync, server, auth+cross-page, live Playwright smoke). Findings → section below.
- [ ] Phase 2 — triage findings, pick fixes + content plan (recorded below before implementation).
- [ ] Phase 3 — implement fixes.
- [ ] Phase 4 — implement new content (challenges, flows, hub widget, lessons-if-gaps).
- [ ] Phase 5 — minify + version bumps + full Playwright verify + typecheck + control-byte check.
- [ ] Phase 6 — commit(s), push, PR.

## Facts pinned during recon

- HTML files are minified IN PLACE (academy.html is one line, ~?KB; index.html 42 lines) — the
  minified HTML *is* the source. Edit with care, re-minify CSS/JS only (HTML edits are already min).
- Current counts (verified): 99 `AcadLabs.register` calls in academy-labs.js, 99 unique
  `data-lab` values in academy.html, stat chips say "99 hands-on labs" / "133 lessons" /
  "12 tracks" / "120+ diagrams" on both academy + index.
- academy-auth.js has its own version line (v=2.6) separate from the 5.76 pair.
- Baseline `npm test`: 38/38 pass (tests/: certificate-serial, exam-draw, login-origin,
  minified-assets, progress-reset, sweep-claim). `npm run` has build:css/js (incl. auth)/html
  scripts and `worker:typecheck`; node_modules present.
- Six audit agents launched (academy HTML structure; functions.js; academy-labs.js+exam-key sync;
  server/Worker; academy-auth.js+cross-page; Playwright live smoke). All read-only.

## Audit findings (Phase 1)

### Agent C — academy-labs.js + exam-key sync (DONE, clean)

No real bugs. Verified: 99 unique `AcadLabs.register` ids; 12 well-formed flows; 5 challenges
(refs r1-bff, w2-wif, r3-tenancy, atk2-fatigue, c3-social — all valid); exam pool 96 Qs, 8/track,
**all 96 (id, a) pairs byte-equal with server EXAM_ANSWER_KEY**; client submits pre-shuffle `oi`,
server grades — correct end to end; ATTEMPTS_PER_DAY 3 == server; no old certId remnants; timers
all cleaned on Reset; no network calls outside lab-exam (which only uses AcademyAuth); no
innerHTML/XSS sinks (only `innerHTML=''` clears + `el()` textNode builder); TOTP/k-anonymity/
ECDSA/salted-SHA-256 crypto all correct; RFC status codes spot-checked correct (12 labs).
Note: lab-exam ALSO has a "finish every lesson" gate before the sign-in gate (existing behavior,
not in CLAUDE.md's description).

Cosmetic only: (1) dead `answered` var in lab-challenge (~L10799); (2) `httpCard()` buckets ≥400
into class `s4xx` — fine today (no 5xx used), rename only if adding 5xx cards.

Planning input: full lab inventory captured (99 labs) — challenges currently cover BFF storage,
WIF, tenancy, MFA fatigue, social engineering; flows cover oidc-code, webauthn, refresh-rotation,
dpop, backchannel-logout, saml-sp, saml-idp, client-creds, device, token-exchange, ciba,
scim-provision.

### Agent A — academy.html structure (DONE, 1 real defect cluster)

PASS: 133 lessons / per-track counts / data-num sequences / chnum text / hub cards+TOCs
(perfect) / stat chips truthful (labs 99, diagrams actually 128 ≥ "120+") / zero broken anchors
across academy+index+3 service pages / zero hardcoded SVG colors / cast clean / widget ids +
flowx-next chain correct / f11-glossary special case intact.

**FIX-1 (high): 4 broken quiz "Track complete" handoffs** — stale from the 2026-07-13 reorder:
- f12-quiz: no link into authn at all → add handoff to `#a0-start`.
- atk9-quiz: links backward to `#f7-itdr`/`#o2-siem`, never mentions ciam → add handoff to `#c0-start`.
- c8-quiz: zero hrefs in box (text anchor: "You now design identity for the people who choose you") → add `#w0-start`.
- w7-quiz: zero hrefs (anchor: "Machines with identities worth trusting — now go design the real thing.") → add `#r0-start`.
Note: 6 other quizzes link the next track's lesson 1 (contextual prose) rather than the intro —
working links, leave as-is; only repair the 4 broken ones (link `*0-start`, matching b9-quiz).

Non-issues noted: all 12 quizzes carry a generic `.acad-try` (uniform, deliberate); summaries
never link their own intro (12/12 uniform); Google-Zanzibar/Drive + Slack mentions are
citations/analogies, not IdP examples — leave.

### Agent D — Worker/server (DONE, 3 fixables, no security drift)

**FIX-2 (med)** `worker.ts:77`: `void maybeSweepExpiredExamIpHashes(env.DB, p => ctx.waitUntil(p))` —
the sweep's CLAIM (claimSweepWindow D1 write) races the response un-protected; on D1-free fast
routes (/auth/start) it can be torn down, silently skipping that hour's scrub (self-healing but
breaks the documented retention bound). Fix:
`ctx.waitUntil(maybeSweepExpiredExamIpHashes(env.DB, p => ctx.waitUntil(p)).catch(() => undefined))`.

**FIX-3 (low)** `api.ts:940-944`: `waitUntil(scrubExamAttemptIpHashesBefore(...))` lacks
`.catch(() => undefined)` (sibling at api.ts:324 has it) — async D1 blip becomes unhandled
rejection, contrary to its own comment.

**FIX-4 (low)**: MAX_STORED_LESSON_ROWS/MAX_STORED_TRACK_ROWS (api.ts, 400/40) duplicated as
MAX_LESSON_ROWS_SQL/MAX_TRACK_ROWS_SQL (store.ts) with no compiler link — extract shared exported
constants so they cannot drift (drift would silently drop writes after a 200).

VERIFIED OK: routing/run_worker_first exact; CSRF guard + same-origin branch sound; cookie
hostname-keying incl. [::1]; OIDC RP (PKCE floor, state-before-error, alg pinned, constant-time
compares, aud/azp, closing-page onError, byte-equal acad_auth_event_v1); back-channel logout
verification strict; epoch re-checked inside SQL; updatedAt re-serialized server-side + 10-min
future cap; row ceilings dual-enforced; exam limits share one windowStart across count/oldest/
scrub; serial 60-bit unbiased CSPRNG; certs fail closed; issuance race-safe; security.ts ==
_headers (intentional CSP asymmetry documented); .assetsignore complete; deploy.yml three-policy
secrets logic + kid continuity fail closed; tsc 0 errors; 38/38 tests. /auth/* JSON routes carry
no CSP (nosniff covers it) — defense-in-depth note only, leave.

### Agent B — functions.js (DONE, 5 fixables + doc drift)

**FIX-5 (high)** functions.js:1554-5: `backBtn.addEventListener('click', showHub)` — event object
becomes focusId, dropPosition undefined → top "All tracks" (#acadBack) never writes the
tombstone; Resume banner/cross-device merge resurrects the backed-out lesson. Fix: wrap as
`function () { showHub(null, true); }` (bottom `__hub__` button already does it right).

**FIX-6 (med)** resetAllProgress() (functions.js:1730-54) never calls renderResumeBanner() —
signed-out learner keeps a live stale Resume banner after Reset-all. Fix: add renderResumeBanner()
after updateProgress().

**FIX-7 (med)** runSearch (functions.js:1898-1921): search state never reset on navigation —
return to hub shows stale results pane, track grid + persona paths stuck hidden. Fix: clear
search.value + reset grid/paths/results visibility in showHub().

**FIX-8 (med, timing)** showLesson→saveRead→scheduleSync fires an 800ms plain sync during
deep-link boot BEFORE AcademyAuth.ready()/firstSync — posts epoch 0, reset-account server reply
applied authoritatively, wiping the anonymous progress claimAnonymousProgress() was meant to
carry over (third unguarded entry point of a documented bug class). Fix: gate scheduleSync on the
first-sync/claim having resolved (e.g. defer until firstSync completes).

**FIX-9 (low)** functions.js:1907-8: search query `q` interpolated into innerHTML unescaped —
self-XSS today, reflected-XSS the day search gets URL wiring. Fix: escape or build via textContent.

**FIX-10 (docs)** CLAUDE.md drift: `.acad-hub-foot`/`#acadResetAll` don't exist (real:
`#acadResetAllTop` in `.acad-hub-progress`); Key Functions `toggleAndSaveTheme()`/
`initSmoothScrolling()`/`initBootstrapComponents()` don't exist (real: `applyTheme()`, CSS
scroll-behavior, no Bootstrap-component init). Update CLAUDE.md.

VERIFIED OK: theme picker/hover, scroll-spy, marquee, boot loader, TRACK_LABELS, epoch+generation
machinery, read-marking gates, update toast, tour selectors+gating, claim single-flight, resume
banner render paths, listener hygiene, null-safety on non-academy pages. PERSONA_PATHS ids all
valid per Agent A's inventory (cross-checked: every id exists).

### Agent E — academy-auth.js + cross-page (DONE, page fixables + doc rewrites)

**FIX-11 (med)** academy.html ships og:image but NO twitter:image (index + 3 service pages have
both) — X/Twitter shares of /academy lose the card. Fix: add twitter:image (+ alt) to academy.html.

**FIX-12 (med)** site.webmanifest is fully orphaned — zero `<link rel="manifest">` anywhere, and
its own name/short_name are empty strings. Fix: fill names + link it from the pages.

**FIX-13 (low)** support/privacy/terms/cancellation have no canonical + og:url (verify has them);
all 4 are in sitemap.xml. Fix: add extensionless canonicals.

**FIX-14 (low)** favicon drift: legal pages ship only favicon.ico; main-chrome pages ship
favicon.ico + IntegrAuth.svg + apple-touch-icon. Fix: unify legal pages up.

**FIX-15 (low)** terms.html was never html-minified (2513 lines vs 0 everywhere else) — the root
cause of the "optimize terms.html 226KB" TODO. Fix: run html-minifier-terser on terms.html.

**FIX-16 (low)** academy-labs.js:7015-7028: exam sign-in passes `onSuccess` that signIn() never
accepts (page redirects away regardless) + stale "does not reload the page" comment. Fix: drop the
dead callback + fix comment.

**FIX-17 (docs)** CLAUDE.md accounts section describes the RETIRED popup sign-in (window.open,
5-min timeout, localStorage handshake as primary channel). Reality: full-page redirect
(`/auth/start?mode=redirect`), no window.open/setTimeout in academy-auth.js at all; cross-tab
propagation rides the SESSION_KEY storage event; server keeps mode=popup supported but unused.
Also stale: Learn ▾ has 3 items (Academy, The Lab ↗, Verify a Certificate); FAQ JSON-LD
divergences are Q1/Q2/Q3/Q6 (doc lists only Q1/Q3 patterns; Q2 drops "See how we engage.",
Q6 drops "— see MCP Security"). Rewrite those CLAUDE.md passages.

VERIFIED OK: AUTH_EVENT_KEY byte-equal; stash/reconcile/generation/claim logic matches design
exactly; API client shapes match api.ts; retry-toast contract; null-safety; #acadAuthNav +
.site-footer byte-identical (md5) on all 11 pages; beacon on all 11 (EOF exceptions exactly
support/privacy/cancellation); all 4 .min files byte-fresh; versions all 5.76/2.6 everywhere;
sitemap/robots/security.txt/mailtos/no-.html-links all clean; attempts/sign-in copy consistent.

_(pending: Playwright smoke)_

## Decisions (Phase 2)

### Bug fixes (all confirmed findings adopted)

Server: FIX-2 (waitUntil the sweep claim), FIX-3 (.catch on scrub), FIX-4 (shared row-ceiling
constants). Front-end: FIX-5 (#acadBack drops position), FIX-6 (banner after reset-all), FIX-7
(search state reset in showHub), FIX-8 (gate scheduleSync until first sync settles), FIX-9
(escape search query), FIX-16 (drop dead onSuccess + stale comment in lab-exam), dead `answered`
var in lab-challenge. Pages: FIX-1 (4 quiz handoffs → next track's `*0-start`), FIX-11
(twitter:image on academy), FIX-12 (webmanifest: fill names + link from pages), FIX-13
(canonicals on 4 legal pages), FIX-14 (favicon set unified), FIX-15 (minify terms.html, in
Phase 5). Docs: FIX-10 + FIX-17 (CLAUDE.md rewrite: redirect sign-in, #acadResetAllTop, real
key functions, Learn ▾ 3 items, FAQ JSON-LD Q1/Q2/Q3/Q6). Leave: s4xx class name, Google/Slack
mentions, quiz .acad-try boxes, li serialization, /auth/* JSON CSP.

### New content (Phase 4)

1. **4 new Flow Explorer flows** (12 → 16): `par` (RFC 9126 — nothing tamperable in the front
   channel), `mtls-bound` (RFC 8705 client auth + certificate-bound tokens — complements dpop),
   `rp-logout` (RP-Initiated Logout 1.0 + back-channel fan-out — literally what this site does),
   `mcp-oauth` (MCP client authorization: 401 + RFC 9728 PRM discovery → PKCE → RFC 8707
   audience-bound token — flagship-service tie-in).
2. **5 new Challenge scenarios** (5 → 10), covering flaw areas the existing five don't:
   redirect-URI wildcard + missing PKCE (ref t7-birth), skipped `aud` check in JWT validation
   (ref t8-validation), device-code phishing via helpdesk (ref atk3-devicecode), offboarding
   drift — an account SCIM never knew about (ref o8-recon), prompt-injection exfil through an
   over-scoped agent token (ref ai9-injection).
3. **New hub widget: Daily drill** (`#acadDrill`, `data-lab="lab-drill"`), placed between
   Challenge mode and Final Exam as exam prep. 5 questions/day drawn from ACAD_EXAM_POOL,
   Leitner-style spaced repetition (wrong answers come back sooner), day streak. State in
   localStorage `acad_drill_v1`, LOCAL-ONLY by design (progress tables have no drill schema and
   schema changes are out of scope) — cleared by Reset-all, NOT by the account-ownership wipe
   (like acad_tour_seen; it is practice stats, not account progress). Chain updates:
   #acadChallenge's next button `__exam__` → `__drill__`; new `__exam__` button in #acadDrill;
   HUB_SECTIONS + tour step + pager-final data-goto text updated.
4. **2 new lessons** (133 → 135; real gaps, both standards-based + vendor-neutral):
   - `p10-fapi` "Locking down OAuth — PAR, JAR & the FAPI profile" (proto, display num 8;
     p9-summary → 9, p8-quiz → 10). Lab `lab-fapi` embeds the new `par` flow via h.flowPlayer +
     a harden-the-flow toggle set.
   - `c10-wallets` "Wallets & verifiable credentials — portable identity" (ciam, display num 8;
     c9-summary → 9, c8-quiz → 10). Lab `lab-vc`: issue → selective disclosure → verify/revoke
     simulation.
   Renumber ONLY the two tracks' summary+quiz data-num/chnum/TOC numbers (ids stay stable, per
   the established insert-before-quiz pattern); update both summaries to link the new lessons;
   hub card counts 10→11 for proto and ciam; stat chips: lessons 135, labs 99→102 (lab-drill,
   lab-fapi, lab-vc), diagrams still "120+" (128→130); "0/135 lessons read"; index promo counts.
   Exam pool unchanged (8/track invariant is per-track, not per-lesson).

### Version plan (Phase 5)

styles/functions/labs + HTML: 5.76 → 5.77 everywhere incl. `<meta acad-build>` +
academy-version.txt (same commit). academy-auth.js untouched → stays 2.6. Minify terms.html
(FIX-15). Full Playwright verify, tsc, npm test, control-byte check before push.

## Implementation log (Phases 3–5)

- DONE FIX-2/3/4 (server): waitUntil'd sweep claim, .catch on scrub, shared row-ceiling
  constants (store.ts exports, api.ts aliases). tsc + 38/38 tests green.
- DONE FIX-5/6/7/8/9 (functions.js): #acadBack wraps showHub(null,true); reset-all repaints
  Resume banner; resetHubSearch hook cleared from showHub; acadFirstSyncSettled boot gate on
  scheduleSync (lifted by firstSync + confirmed auth events); escapeHtml on search output.
- DONE FIX-16 + dead `answered` var (academy-labs.js).
- DONE drill navigation wiring (functions.js): __drill__ sentinel, acadDrill anchor, tour step
  5 (+renumbered 6/7), tour copy 135 lessons / ten scenarios, clearLocalProgress clears
  acad_drill_v1 (reset-all only; ownership wipe untouched), stale clearLocalProgress comment
  corrected.
- DONE Playwright smoke (pre-change): 35 clean page runs; 2 new defects found and fixed —
  **FIX-18** theme toggle click-after-hover closed the menu (click now confirms; wrap in
  functions.js), **FIX-19** @media print body rule lost the `!important` specificity tie to
  `body.bg-dark.theme-cyber` (print block now repeats the theme selectors; later-in-file wins).
- DONE Phase 4 content: academy-labs.js grew to 102 registers / 16 flows / 10 challenges
  (agent-built, self-verified with a 70-assertion browser pass); `.acad-drill-*` CSS +
  `#acadDrill` in the print hide-list; academy.html integration script applied 31 asserted
  replacements + card-scoped count updates (135 lessons, 102 labs, 130 figures, proto/ciam ×11,
  chain Flow Explorer→Challenge→Drill→Exam, 4 quiz handoffs repaired); index promo chips + FAQ +
  JSON-LD updated; head fixes FIX-11..14 applied (academy twitter:image; webmanifest named +
  linked on all 11 pages; canonicals on 4 legal pages; favicon set unified); server comments
  133→135; CLAUDE.md rewritten (FIX-10/17 + all new counts/surfaces).
- DONE Phase 5 build: **FIX-15 root-caused and fixed** — terms.html was never minified because
  a stray '<' in `<bdt ...><IntegrAuth</bdt>` hard-errors html-minifier AND made browsers
  swallow the company name on the live page ("We are , doing business as…"); bracket removed,
  terms minified 236KB→183KB with byte-identical visible text (8,305 words). GOTCHA for next
  time: in-place `--input-dir . --output-dir .` minification TRUNCATES every page to 0 bytes
  when one file errors — minify per-file to a temp path and replace on success only. All assets
  re-minified; ?v=5.77 on all 11 pages + acad-build + academy-version.txt (auth stays 2.6);
  38/38 tests, tsc, control-byte check green.
- IN FLIGHT: final full Playwright verification (15-point checklist incl. every fix regression
  and all new content) — PR opens on its pass.
