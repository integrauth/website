@AGENTS.md

<!-- This repo's instructions for AI coding agents live in AGENTS.md (the
     tool-agnostic file other agents read too). This file exists only so
     Claude Code loads them automatically via the @-import above. -->
